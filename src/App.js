import { BrowerRouter, Route, Switch} from "react-router-dom";

function App() {
  return (
    <BrowerRouter>
    <switch>
      <Route component={Home} path='/' />
      <Route component={} path='' />
      <Route component={} path='' />
      <Route component={} path='' />
      <Route component={} path='' />
    </switch>
    </BrowerRouter>
  )
}

export default App;
